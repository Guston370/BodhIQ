<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions for Google Sign-In and Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permissions for medical report scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Permissions for medicine reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Camera feature (optional) -->
    <uses-feature 
        android:name="android.hardware.camera" 
        android:required="false" />

    <application
        android:name=".BodhIQApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BodhIQ">

        <!-- Splash Activity - App entry point -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BodhIQ.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar" />

        <!-- Sign Up Activity -->
        <activity
            android:name=".ui.login.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar"
            android:parentActivityName=".ui.login.LoginActivity" />

        <!-- Forgot Password Activity -->
        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar"
            android:parentActivityName=".ui.login.LoginActivity" />

        <!-- Main Activity - Simple demo activity after login -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar" />

        <!-- Integration Test Activity - For development testing -->
        <activity
            android:name=".integration.IntegrationTestActivity"
            android:exported="true"
            android:theme="@style/Theme.BodhIQ.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Text Result Activity - OCR results display -->
        <activity
            android:name=".ui.TextResultActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar"
            android:parentActivityName=".MainActivity" />

        <!-- Report Details Activity - Detailed report view -->
        <activity
            android:name=".ui.ReportDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar"
            android:parentActivityName=".MainActivity" />

        <!-- FileProvider for sharing PDF files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Reminder Receivers -->
        <receiver
            android:name=".receivers.ReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".receivers.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Reminders Activity -->
        <activity
            android:name=".ui.reminders.RemindersActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar"
            android:parentActivityName=".MainActivity" />

        <!-- Emergency QR Activity -->
        <activity
            android:name=".ui.EmergencyQrActivity"
            android:exported="false"
            android:theme="@style/Theme.BodhIQ.NoActionBar"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>