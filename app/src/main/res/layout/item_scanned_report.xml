<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:background="@drawable/ripple_background"
    android:elevation="4dp"
    android:orientation="horizontal"
    android:padding="16dp">

    <!-- Status Indicator -->
    <View
        android:id="@+id/view_status_indicator"
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="16dp"
        android:background="?attr/colorTertiary" />

    <!-- Report Image (Optional) -->
    <ImageView
        android:id="@+id/image_report"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/image_placeholder_background"
        android:contentDescription="Report image"
        android:scaleType="centerCrop"
        android:visibility="gone"
        tools:src="@drawable/ic_document"
        tools:visibility="visible" />

    <!-- Report Content -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

            <!-- Report Title -->
            <TextView
                android:id="@+id/text_report_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/primary_text"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="Blood Test Report - March 2024" />

            <!-- Report Date -->
            <TextView
                android:id="@+id/text_report_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/secondary_text"
                android:textSize="14sp"
                tools:text="Mar 15, 2024 • 10:30 AM" />

            <!-- Health Values Count -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/icon_health_values"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_marginEnd="6dp"
                    android:src="@drawable/ic_health"
                    android:tint="@color/primary_color"
                    android:visibility="gone"
                    tools:visibility="visible" />

                <TextView
                    android:id="@+id/text_health_values_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/primary_color"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:visibility="gone"
                    tools:text="5 health parameters detected"
                    tools:visibility="visible" />

            </LinearLayout>

            <!-- Report Preview Text -->
            <TextView
                android:id="@+id/text_report_preview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/secondary_text"
                android:textSize="14sp"
                tools:text="Complete Blood Count (CBC) test results showing hemoglobin levels, white blood cell count..." />

    </LinearLayout>

    <!-- Chevron Icon -->
    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp"
        android:alpha="0.6"
        android:src="@drawable/ic_chevron_right"
        android:tint="@color/secondary_text" />

</LinearLayout>